import { createServerClient } from "@supabase/ssr";`nimport { cookies } from "next/headers";`n`nexport async function createClient() {`n  const cookieStore = await cookies();`n  return createServerClient(`n    process.env.NEXT_PUBLIC_SUPABASE_URL!,`n    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,`n    {`n      cookies: {`n        getAll() { return cookieStore.getAll(); },`n        setAll(cookiesToSet: {name: string; value: string; options?: any}[]) {`n          try { cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)); } catch {}`n        },`n      },`n    }`n  );`n}
